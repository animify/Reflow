#renderer, #renderer-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;

    image {
        pointer-events: none;
    }
}

#renderer-overlay {
    pointer-events: none;
}